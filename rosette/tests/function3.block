----------------------- CORE 0(0,0) ------------------------

----------------------- CORE 1(0,1) ------------------------
int@1 sum(int@1 d) {
  return (read(S) +@1 d);
}

void main() {
  int@1 ret;
  ret = sum(read(S));
  ret = sum(read(S));
}


----------------------- CORE 2(0,2) ------------------------

----------------------- CORE 3(0,3) ------------------------

----------------------- CORE 4(0,4) ------------------------

----------------------- CORE 5(1,0) ------------------------
void sum(int@5 b) {
  send(E,(read(E) +@5 b));
}

void main() {
  int@5 a;
  int@5 b;
  send(E,a);
  sum(b)

  // fix deadlock here
  send(E,b);
  send(E,a);
  sum(read(E))
}


----------------------- CORE 6(1,1) ------------------------
void sum(int@6 a,int@6 c) {
  send(W,a);
  send(N,(read(W) +@6 c));
}

void main() {
  int@6 c;
  int@6 d;
  send(N,d);
  sum(read(W), c)

  // fix deadlock here
  send(W,c);
  send(N,read(W));
  sum(d, read(W))
}


----------------------- CORE 7(1,2) ------------------------

----------------------- CORE 8(1,3) ------------------------

----------------------- CORE 9(1,4) ------------------------

----------------------- CORE 10(2,0) ------------------------

----------------------- CORE 11(2,1) ------------------------

----------------------- CORE 12(2,2) ------------------------

----------------------- CORE 13(2,3) ------------------------

----------------------- CORE 14(2,4) ------------------------

----------------------- CORE 15(3,0) ------------------------

----------------------- CORE 16(3,1) ------------------------

----------------------- CORE 17(3,2) ------------------------

----------------------- CORE 18(3,3) ------------------------

----------------------- CORE 19(3,4) ------------------------

