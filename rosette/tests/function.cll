int abs(int x) {
  if(x < 0) {
    return -x;
  } else {
    return x;
  }
}

int square_distance(int x, int y) {
  return (x - y) * (x - y);
}

int main() {
  int@1 x;
  int@2 y;
  return abs(x) + abs(y) * square_distance(x,y);
}
