// c

void main() {
  fix14_t sum;
  fix14_t a[8] = {0, 1, 2, 3, 4, 5, 6, 7}; // 8X8
  fix14_t s[8] = {1, 2};
        sum = 0;
      for (k from 0 to 4) {//for each state
        for (l from 0 to 2) {//for each state
          sum = sum + s[l] * a[(4 * l) + k];
        }
      } 
  out(sum);
}
