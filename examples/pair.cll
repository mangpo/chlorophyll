int::2@(2,2) func(int::2@(2,2) x) {
  return 10;
}

int::2@(2,2) func2(int z, int::3@(2,2,2) x) {
  int::2 a;
  a::0 = x::0;
  a::1 = z;
  return a;
}

void main() {
  int::2@(0,1) a, b, x;
  x = a +@(0,1) b;
  a = in();
  a::1 = in();

  int::2[]@({[0:5], [5:10]},{[0:10]}) y[10];
  y[a::0] = y[1] +@({[0:5], [5:10]; i},{[0:10]; i}) y[2];
  y::0[a::0] = y::0[1] +@{[0:5], [5:10]; i} y::0[2];

  x = y[a::0];

  x = func2(1, func(0));
}
