int::2 func(int::2 x) {
  return 0;
}

void main() {
  int::2@(0,1) a, b, x;
  x = a +@(0,1) b;
  a = in();
  a::1 = in();

  int::2[]@({[0:5]=0, [5:10]=0},{[0:10]=1}) y[10];
  y::0[a::0] = y::0[1] +@({[0:5]=0, [5:10]=0; i},{[0:10]=1; i}) y::0[2];

  x = y[a::0];

  x = func(0);
}
