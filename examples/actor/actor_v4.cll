// -*- c -*-

# 0 --> 000
# 1 --> 100
# 2 --> 200
actor edge@(0~>1);
actor edge@(1~>2);

int@0 edge(int@2 a){
  int@1 b;
  return a +@2 b;
}

void main(){
  int@0 x, b;
  x = 0;
  while(1) {
  if(b >@0 0) {
    x = edge(x);
  }
  }
}
