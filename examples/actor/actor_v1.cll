// -*- c -*-

# 0 --> 000
# 2 --> 100
actor edge@(0~>2);

int@0 edge(int@2 a){
  return a +@2 1;
}

void main(){
  int@0 x, b;
  int@2 self;
  self = 999;
  b = 0;
  x = 0;
  while(b < 10) {
    if(b >@0 5) {
      x = edge(x);
      out(x);
    }
    b = b+1;
  }
}
