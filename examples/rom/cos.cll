void main() {
  int@0 x;
  int@0 y;
  int coeff[4] = {-281 , 5203 , -42329 , 37407};
  int@0 sum;

  x = in() << 2;
  if(x < 0) {
    x = x + 65536;
  } else {
    x = 65536 - x;
  }

  y = x */16 x;
  sum = 0;
  for(i from 0 to 4) {
    sum = sum */16 y + coeff[i];
  }

  out(sum */16 x + x);
}
