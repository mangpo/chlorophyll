int::2@(2,2) func(int::2@(2,2) x) {
  return 10;
}

int::2@(2,2) func2(int z, int::3@(2,2,2) x) {
  int::2 a;
  a::0 = x::0;
  a::1 = z;
  return a;
}

void main() {
  int::2 a, b, c;
  c = a + b;
  a = in();
  a::1 = in();

  int::2[]@({[0:5]=1, [5:10]=1},{[0:10]}) y[10];
  y[a::0] = y[1] + y[2];
  y::0[a::0] = y::0[1] + y::0[2];

  c = y[a::0];

  c = func2(1, func(0));
}
