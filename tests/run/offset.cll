void main() {
  int[]@{[0:2]=0,[2:4]=0} x[4];
  int[]@{[0:2]=1,[2:4]=0} y[4];
  for(i from 0 to 4) {
    y[i] = in();
  }

  for(i from 0 to 4) {
    x[i] = y[i];
  }

  for(i from 0 to 4) {
    out(x[i]);
  }
}
