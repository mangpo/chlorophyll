int@0 sum(int@2 a,int@2 b,int@2 c,int@0 d) {
  return a +@2 b +@2 c +@0 d;
}

void main() {
  int@1 a;
  int@1 b;
  int@2 c;
  int@2 d;
  int@0 ret;
  a = in();
  b = in();
  c = in();
  d = in();
  ret = sum(a,b,c,0 + sum(d,c,b,a));
  out(ret);
}
